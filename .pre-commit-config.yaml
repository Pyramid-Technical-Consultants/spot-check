# Same checks as CI (.github/workflows/ci.yml). Install once after setup:
#   pre-commit install
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.11.12
    hooks:
      - id: ruff-check
        args: [--fix]
        files: ^(src|tests|packaging)/
